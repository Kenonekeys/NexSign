# NexSign Web UI

The NexSign web interface is built in Rust and WebAssembly and lives in the main repository under `/src`.

You can embed the live NexSign UI directly inside mdBook:

<iframe src="https://kenonekeys.github.io/NexSign/" width="100%" height="800px"></iframe>

This allows users to interact with NexSign without leaving the documentation.
